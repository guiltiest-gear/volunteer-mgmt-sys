name: Lint Markdown
on:
  push:
    paths:
      - '**/*.md'
      - '.rumdl.toml'

jobs:
  rumdl:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: rvben/rumdl@v0
